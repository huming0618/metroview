{
  "name": "metroview",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Metro View Android (Capacitor) with offline MVP — mobile OSM metro/subway viewer",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:android": "VITE_BASE=./ vite build && npx cap sync android",
    "preview": "vite preview",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "seed-offline-tiles": "node scripts/download-offline-tiles.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "leaflet": "^1.9.4",
    "leaflet.markercluster": "^1.5.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "vite": "^6.0.0"
  }
}
